import { BubblingDirective } from './bubbling.directive';

describe('BubblingDirective', () => {
  it('should create an instance', () => {
    const directive = new BubblingDirective();
    expect(directive).toBeTruthy();
  });
});
